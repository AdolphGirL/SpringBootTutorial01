package com.reyes.tutorial.config;

public class CrudRESTfulBase {
	
	/**
	 * RESTful規範
	 * - uri: /資源名稱/資源標示 ，請求區分
	 * 
	 *   GET	查
	 *   POST	新
	 *   PUT	修
	 *   DELETE	刪
	 *   
	 *   所有複數 emps
	 *   單一	 emp/{id}
	 *   
	 *   添加頁面
	 *   emp get
	 *   添加
	 *   tmp post
	 *   
	 *   修改頁面
	 *   emp/{id} get
	 *   
	 *   修改
	 *   emp put
	 *   
	 *   刪除
	 *   emp/{id} get
	 *   
	 *   刪除
	 *   emp/{id} delete
	 */

}
